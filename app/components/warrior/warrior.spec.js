
	describe('The Warrior component viewmodel', function(){

		it('should default the size to small if no parameter is provided');

		it('should set the size properly if the parameter is provided');

		it('should default the image to empty string if no parameter is provided');

		it('should set the image properly if the parameter is provided');

	});
