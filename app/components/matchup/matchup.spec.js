    describe('the matchup viewmodel', function() {

        it('should not display opponents when less than 2 warriors exist in the model');

        it('should choose 2 unique warriors as opponents');

        it('should emit selection event and select new opponents when opponent1 is selected');

        it('should emit selection event and select new opponents when opponent2 is selected');

    });

